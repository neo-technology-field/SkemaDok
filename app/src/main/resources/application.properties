# SkemaDok embedded server — only active when running the 'ui' subcommand
server.port=8282

# Suppress Spring Boot startup banner for a clean CLI experience
spring.main.banner-mode=off

# Reduce startup log noise; errors still surface
logging.level.root=WARN
logging.level.com.neo4j.skemadok=INFO
